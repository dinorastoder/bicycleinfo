var expect = require { expect } from 'chai';
import { BikeService } from "./bike";

describe('BikeService construct()', function () {
  it('create', function () {
    

    let bs = new BikeService();
    bs.create()
    expect(sum2).to.be.equal(sum1);

  });
});